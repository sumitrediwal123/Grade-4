# Task:- Create an elif statement that checks if the shape is "circle". Inside this block, use input() to ask the user for the radius of the circle

import turtle

# Take the input from the user (square/circle)
shape = input("Enter shape to draw (square / circle): ")

# Decision making
if shape == "square":
    side = int(input("Enter the side length: "))
    # drawing square code will go here


elif shape == "":
    radius = 
    # drawing circle code will go here


else:
    print("Invalid shape!")
